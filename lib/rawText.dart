import 'package:flutter/material.dart';

class Rawtext extends StatelessWidget {
  const Rawtext({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Raw Text"));
  }
}
