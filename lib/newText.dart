import 'package:flutter/material.dart';

class NewTextComponent extends StatelessWidget {
  const NewTextComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("New Text Component");
  }
}
